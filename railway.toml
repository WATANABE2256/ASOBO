[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["asobo-web/**"]

[deploy]
healthcheckPath = "/swipe"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
